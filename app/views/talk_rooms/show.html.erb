<div class="chat-room">
 <h1><%=@receive_user.user.name%>とのメッセージ</h1>
  
 <hr>
 <div class="chats">
   <div class="chat">
     <% if @messages.present? %>
       <% @messages.each do |m| %>
         <div class="chat-box">
           <div class="chat-text">
            <div class = "chat-name-bar">
              <% if m.user.image.nil? %>
                <img class = "chat-icon-image" alt = "デフォルトアイコン" src = "https://presite-potepancamp-rails-02.herokuapp.com/assets/default_icon-9263fc59c414b7228d256fc178dcb22183561357950a68f5ad086ba7ee054974.jpg">
              <% else %>
                <img class = "chat-icon-image" alt = "アイコン" src = "<%= "/user_images/#{m.user.image}" %>">
              <% end %>
            </div>
            <div class = "chat-name-bar"><%= m.user.name %></div>
            <div class = "chat-name-bar"><%= m.created_at.strftime("%Y-%m-%d %H:%M") %></div><br>
            <%= m.body %>
           </div>
         </div>
       <% end %>
       <%else%>
         <p>メッセージはまだありません</p>
     <% end %>
   </div>
   
   <div class="posts">
     <%= form_with model: @message, local: true do |f| %>
       <%= f.text_field :body, class: "send-form", placeholder: "メッセージを入力してください" %>
       <%= f.hidden_field :talk_room_id, value: @room.id %>
       <%= f.submit "送信する", class: "send-btn" %>
     <% end %>
   </div>
 </div>
</div>
